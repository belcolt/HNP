@model IEnumerable<HospiceNiagara.Models.Event>

<ul class="list-group">
    
    @foreach (HospiceNiagara.Models.HospiceDate item in ViewBag.MeetingList)
    {
        <li class="list-group-item">
            @item.Name
        </li>
    }
    
</ul>

